{
  "name": "slconnect",
  "version": "1.0.0",
  "description": "A monorepo for SLChatApp with backend, mobile, and shared packages",
  "main": "index.js",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "start": "pnpm -r --parallel start",
    "test": "pnpm -r test",
    "install:all": "pnpm install"
  },
  "keywords": [
    "chat",
    "app",
    "monorepo"
  ],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.13.1",
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
